[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-sudo-mcp"
version = "0.5.6"
description = "Authorization, delegation, provenance, and verifiable-audit engine for AI agents"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [{name = "agent-sudo contributors"}]
dependencies = []
keywords = [
    "mcp",
    "model-context-protocol",
    "ai-agents",
    "security",
    "zero-trust",
    "human-in-the-loop",
    "audit-logs",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.optional-dependencies]
dev = ["pytest", "ruff"]
# Examples only — never a runtime dependency of agent-sudo-mcp, which stays
# dependency-free. Needed to run examples/pydantic_ai.
examples = ["pydantic-ai>=1.0"]

[project.urls]
Homepage = "https://github.com/Kisyntra/Agent_Sudo"
Repository = "https://github.com/Kisyntra/Agent_Sudo.git"
"Bug Tracker" = "https://github.com/Kisyntra/Agent_Sudo/issues"
Documentation = "https://github.com/Kisyntra/Agent_Sudo#readme"

[project.scripts]
agent-sudo = "agent_sudo.gateway:main"
agent-sudo-mcp = "agent_sudo.mcp_server:main"

[tool.setuptools.packages.find]
include = ["agent_sudo*"]

[tool.setuptools.package-data]
agent_sudo = ["config/*.yaml", "examples/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
